// Объявляем переменные
let age = 25;
let year = 2019;

// Выводим переменные
console.log(age,year);

// Перезаписываем age и снова выводим
age = 30;
console.log(age);

// var — старый способ объявления переменных
var score = 75;
console.log(score);

// Константы нельзя перезаписать
const points = 100;
points = 50;
console.log(points); // Получаем ошибку