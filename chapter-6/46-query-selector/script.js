const para = document.querySelector('.error'); //grab first '.error' element
console.log(para);

const paras = document.querySelectorAll('p'); //grab all 'p' elements
console.log(paras);
console.log(paras[2]); //get third 'p' element